#ifndef PASS2_H
#define PASS2_H

#include <string>
#include "pass1.h"

using namespace std;

void runPass2(
    const string& sourceFilename,
    const string& listingFile,
    const string& symtabFile
);

#endif